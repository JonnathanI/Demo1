server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://ep-holy-lab-adscvkyb-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_CnJMka1dSQ5x
    driverClassName: org.postgresql.Driver

  jpa:
    hibernate:
      # CAMBIAR DE 'create-drop' A 'update'.
      # 'update' mantiene los datos y solo ajusta el esquema si cambian las entidades.
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true
    baselineVersion: 1
    locations: classpath:db/migration
    sqlMigrationSuffixes: .sql
    baselineOnMigrate: true
    sqlMigrationPrefix: V
    schemas: public
